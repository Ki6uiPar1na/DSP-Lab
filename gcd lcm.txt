% GCD calculation (Euclidean algorithm)
gcd(X, 0, X) :- X > 0.
gcd(X, Y, GCD) :-
    Y > 0,
    Remainder is X mod Y,
    gcd(Y, Remainder, GCD).

% LCM calculation
lcm(X, Y, LCM) :-
    gcd(X, Y, GCD),
    LCM is (X * Y) / GCD.